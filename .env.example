# --- OBLIGATORIAS ---
DATABASE_URL=
ENCRYPTION_KEY=
HASHID_SALT=gmaps-scraper-pro-default-salt

# --- CONFIGURACIÓN DE RED (Railway) ---
ADDR=:${PORT}

# --- OPCIONALES (Ajustes de rendimiento) ---
DB_MAX_CONNS=10
DB_MIN_CONNS=2
DB_MAX_CONN_LIFETIME=1h

# --- TELEMETRÍA Y DEBUG ---
# DEBUG=1
# DISABLE_TELEMETRY=1
