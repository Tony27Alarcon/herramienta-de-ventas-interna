[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.saas"

[deploy]
startCommand = "/app/gmapssaas serve"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
